server:
  port: 20030

spring:
  profiles:
    active: dev

  r2dbc:
    url: r2dbcs:mysql://192.168.80.129:3306/spring_r2dbc_samples?sslMode=DISABLED
    username: spring_r2dbc_samples_user
    password: B55!3Ufhj

  application:
    name: webflux
  
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss

management:
  endpoints:
    web:
      exposure:
        include: "*"